#include "main.h"

/**
 * _abs - print a number
 * @a: the number to be checked
 *
 * Description: Print absolute number of a
 * Return: a positive number
 */

int _abs(int a)
{
	if (a < 0)
	{
		return (-a);
	}

		return (a);
}
